import { url } from "inspector";

export class QaAdminApis {
    id?: any;
    api-title?: string;
    api-description?: string;
    api-url?: url;
    api-username?: string;
    api-password?: string;
    api-token?: string;
    isPublished?: boolean;
}
